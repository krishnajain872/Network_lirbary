TODO TASKS:

1. Restore Benchmark Count
   - The benchmark count in `oms_client/scenarios/benchmark.cpp` was reduced to 1000 for verification in the debug/sandbox environment.
   - Task: Restore it to 10,000 or make it configurable via CLI/Config.

2. Market Data Integration
   - The `MarketDataPublisher` in `oms_server/main.cpp` currently generates random dummy data.
   - Task: Integrate with a real Order Book or external Market Data feed.

3. TcpHandler Efficiency
   - `core/src/protocols/tcp/tcp_handler.cpp` creates a new `TcpStreamContext` (shared_ptr) for every message.
   - Task: Cache the context in `OnConnection` or inside `Connection` to reduce allocation overhead.

4. Subscriber Cleanup
   - `MarketDataPublisher` in `oms_server/main.cpp` stores `std::shared_ptr<IStreamContext>` indefinitely.
   - Task: Implement a mechanism to remove subscribers when they disconnect. This might require `IStreamContext` to expose connection state or a callback from `TcpHandler` on disconnect.

5. Tracing Overhead
   - Tracing logs (`[TRACER] Span Finished`) appear even when tracing is supposedly disabled in config.
   - Task: Investigate `Tracer` implementation and ensure it respects the `enabled` flag to avoid performance penalty in high-throughput scenarios.

6. Logging Configuration
   - Verify if `Logger::Initialize` hardcoded string in `main.cpp` overrides YAML config fully or partially.
   - Task: Refactor initialization to respect YAML config for output destination and level.
